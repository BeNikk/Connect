import { Button } from "./ui/button";

const CreatePost = () => {
  return (
    <div>
      <Button className="fixed bottom-6 right-10 lg:right-96"> + Post</Button>
    </div>
  );
};

export default CreatePost;
